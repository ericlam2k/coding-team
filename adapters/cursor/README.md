# Cursor adapter

**Not implemented in v1.**

Use the Codex adapter (`adapters/codex`) and `./install.sh --platform codex` for now. A Cursor-native install (skills under `.cursor/`, Task agents, pool map for Cursor slugs) will land in a later version.
