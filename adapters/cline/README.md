# Cline adapter

**Not implemented in v1.**

Use the Codex adapter (`adapters/codex`) and `./install.sh --platform codex` for now. A Cline-native install (`team_*` wiring, role prompts) will land in a later version.
