# Prompt compression

Use to reduce prompt size while preserving operational behavior. Remove
repetition, separate universal from task-specific rules, and retain safety and
tool boundaries. Report retained behavior, material removals, and risk notes.
The Lead may select this workflow only after core routing; it does not select a
profile, model, role, or additional call.
