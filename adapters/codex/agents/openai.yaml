interface:
  display_name: "Coding Team"
  short_description: "Sprint → Batch → Task orchestration with role delegates"
  default_prompt: "Use $coding-team as Lead: classify the request, assign one accountable role, keep WIP≤2 ordinary specialists, and route each concise handoff to the next related role only when an unresolved question remains."
