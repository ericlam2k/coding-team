interface:
  display_name: "PM Lean Assumption Triage"
  short_description: "Rank product assumptions for human review"
  default_prompt: "Use $pm-lean-assumption-triage to rank the assumptions in this PM brief."
policy:
  allow_implicit_invocation: false
