# Handoff

- **Task / role:**
- **Status:** DONE | PARTIAL | BLOCKED
- **Conclusion:**
- **Changed artifacts or decision:**
- **Evidence:**
- **Residual risk or blocker:**
- **Recommended next role or action:**

The finishing role writes this in the same task, using the cheapest capable
mapped slug. Gatekeeper writes `templates/review-decision.md` the same way
from the origin verdict; the cheap slug does not remake the call.
`gatekeeper:small` is unchanged. Do not spawn Docs Steward for this record. If a named UX or
architecture contract existed, next inspect owner is `frontend-ux-lead:inspect`
or `system-architect:inspect` (cheap slug), not the contract premium slug and
not Gatekeeper, unless the work is `small`. Keep it concise enough for Lead to
route without replaying the task. No separate machine validator is required.
