# Handoff

> Cap: **150 words**.

## Meta

- **Task ID / Role ID:**
- **Batch ID:**
- **Status:** DONE | BLOCKED | PARTIAL

## Result

- **What changed / concluded:**
- **Evidence (paths, commands, line refs):**
- **Assignment fields:** `execution_scope`; `reasoning_depth`; `enumeration_required`; `synthesis_input_ref`
- **Bounded evidence:** source set/query; inspected-source count; lookup-session count
- **Checkpoint / escalation reason:** `NONE` or one recorded reason
- **Checkpoint conditions:** second lookup session, source-set expansion, second follow-up, 75% review, 180s without artifact, or unexplained receipt growth
- **Receipt:** input / cached / output, or `TELEMETRY_UNAVAILABLE`, with source
- **Residual risk / unknowns:**

If status is `PARTIAL`, list verified facts, unavailable fields, one unresolved
item, and exactly one next Task. If enumeration is required, the Investigator
packet must be complete before manager synthesis. Managers must not repeat the
lookup.

## Next

- **Recommended next role ID:**
- **Next bounded Task:** (one owner, exclusive scope, one acceptance artifact)
- **Blocked on (human / role / evidence):**

## Runtime / policy receipt

- **Policy cache:** `MISS` | `HIT` | `INVALIDATED` | `BYPASSED` | `UNAVAILABLE`
- **Policy files read/reused/hashed:**
- **Elapsed seconds / source:**
- **Tokens:** `MEASURED` | `ESTIMATED` | `UNAVAILABLE`; input / output / total:
- **Telemetry source:** `host_runtime` | `local_helper` | `unavailable`
- **Persisted event:** `.coding-team/runs/policy-cache-events.jsonl` or `NOT_PERSISTED`

## Tier note (if any)

- **planned → actual:**
