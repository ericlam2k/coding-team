# Distillation entry

Use for a concrete reusable lesson, especially after fallback correction,
Test Engineer failure, user feedback, or Gatekeeper revision/block.

Default data boundary: `storage_scope=LOCAL_ONLY`, `export_status=NOT_REQUESTED`,
`public_safe=false`, `consent_ref=null`, `redaction_check=NOT_RUN`. Keep only
generic sanitized outcomes and evidence references; any public-safe export
requires explicit per-run human consent and a passing redaction check.

## Identity

- **Entry ID:**
- **Source experiment / run / batch:**
- **Role and fallback mode:**
- **Status:** CANDIDATE | SUPPORTED | PROMOTED | CLOSED

## Lesson

- **Problem or useful pattern:**
- **Root cause / missed reasoning step:**
- **Missing evidence:**
- **Higher-tier or reviewer correction:**
- **New durable rule:**
- **Trigger:**
- **Evidence refs:**

## Promotion

- **Target:** ROLE_FALLBACK | PROJECT_RULE | TESTING_RULE | GATEKEEPER_RULE | ARCHITECTURE_RULE | NONE
- **Supporting independent observations:**
- **Validation / falsifier:**
- **Human gate:** pending | approved | rejected | not required
- **Owner / review or expiry date:**
