interface:
  display_name: "System Architecture"
  short_description: "Freeze cross-layer architecture contracts before allocation."
  default_prompt: "Use $system-architecture to freeze the named cross-layer architecture contract without implementing product code."
policy:
  allow_implicit_invocation: false
